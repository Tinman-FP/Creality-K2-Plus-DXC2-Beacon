# Superseded compatibility note
#
# The preheat-only experiment that used to live in this file has been replaced
# by ../macros/dxc2_cfs_tool_alias.cfg. The validated wrapper contains the same
# temperature wait plus physical CFS slot mapping, double cutting, a 400 ms
# dwell, a post-load sensor gate, and one bounded retraction retry.
#
# Do not include this file. It intentionally defines no macros.
