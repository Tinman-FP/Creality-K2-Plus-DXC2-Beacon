# Replace the existing macro with this section.  Do not leave two sections
# named QUIT_MATERIAL_RETRUDE_MATERIAL in gcode_macro.cfg.

[gcode_macro QUIT_MATERIAL_RETRUDE_MATERIAL]
gcode:
  {% if printer["filament_switch_sensor filament_sensor"].filament_detected %}
    G91
    G0 E-40 F360
    G90
    M400
  {% endif %}
