# Beacon remains mandatory for homing and mesh generation. After a completed
# print-start mesh, ignore only this idle USB MCU's communication timeout.
[beacon_guard]
mcu: beacon
require_mesh: True
require_active_print: True

